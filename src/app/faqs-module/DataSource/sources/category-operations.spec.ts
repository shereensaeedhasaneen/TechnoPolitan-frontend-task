import { CategoryOperations } from './category-operations';

describe('CategoryOperations', () => {
  it('should create an instance', () => {
    expect(new CategoryOperations()).toBeTruthy();
  });
});
